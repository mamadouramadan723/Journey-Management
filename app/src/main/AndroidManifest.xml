<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myjourney">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyJourney">
        <activity
            android:name=".main.Activity_Settings"
            android:exported="true" />
        <activity
            android:name=".main.Activity_Login_or_Register"
            android:exported="true" />
        <activity
            android:name=".main.Activity_Main"
            android:exported="true"
            android:theme="@style/Theme.MyJourney.NoActionBar"/>
        <activity
            android:name=".main.Activity_Splash"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyJourney.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>